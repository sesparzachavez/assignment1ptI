public interface Sort {
    /**
     * every class that implements Sort must implement sort and return a sorted double array
     * @param arr
     * @return
     */
    double[] sort(double[] arr);
}
